{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"The Developer Experience Dream","text":""},{"location":"#-to-master-a-web-framework-in-30-minutes-or-less","title":"- To Master A Web Framework In 30 Minutes Or Less","text":"<p>Heaven was born from a simple dream: You shouldn't need a PhD in web development to build an enterprise web application</p> <p>Web development is like a chess board. Many frameworks expect you to be at the level of Magnus Carlsen before you can play (create) a legendary game (web application).</p> <p>Other frameworks are like a crowded room shouting suggestions at you. They try to be everything for everyone. They bury you in layers of abstraction until you forget what HTTP looks like.</p> <p>Heaven is different.</p> <p>It gives you raw, unadulterated power and gets out of your way immediately with a syntax designed for an average developer to understand in 30 minutes or less.</p> <p>yourapp/app.py <pre><code>from heaven import Router\n\n# create the application\napp = Router()\n\n# define a route and tell heaven where it can find your handler\napp.GET('/v1/games/:id', 'controllers.games.play_a_game')\n</code></pre></p> <p>yourapp/controllers/games.py <pre><code>def play_a_game(req, res, ctx):\n  res.body = 'Checkmate.'\n</code></pre></p>"},{"location":"#why-another-python-framework","title":"Why Another Python Framework?","text":"<p>Tired of Flask's thread-local magic and aging internals. Tired of Django's monolithic weight. Tired of FastAPI's dependency injection labyrinth?</p> <p>Looking for something Powerful and lightning Fast. A framework that You won't spend months learning, cos life is too short - and more importantly years mastering?</p> <p>... then you belong in Heaven.</p>"},{"location":"#your-30-minute-path-to-mastery","title":"Your 30-Minute Path to Mastery","text":"<p>Because Heaven relies on standard Python and pure HTTP concepts, you don't need a 500-page manual. You need 30 minutes.</p> <ul> <li>Min 01-02: The Beginning - From Zero to Hello World.</li> <li>Min 03-04: The Command Line - Controlling the skies with <code>fly</code> and <code>run</code>.</li> <li>Min 05-06: The Router - Understanding routes and dispatch.</li> <li>Min 07-08: Subdomains &amp; Mounts - Multi-tenancy and architecture.</li> <li>Min 09-10: The Request - Reading the user's mind.</li> <li>Min 11-12: The Response - Speaking back with authority.</li> <li>Min 13-14: Templating &amp; Files - Rendering views and serving static assets.</li> <li>Min 15-16: The Context - Managing state like a pro.</li> <li>Min 17-18: Hooks &amp; Middleware - Intercepting the pipeline power.</li> <li>Min 19-20: Schemas &amp; Validation - Bulletproof data validation.</li> <li>Min 21-22: Auto-Documentation - Free Swagger UI.</li> <li>Min 23-24: Testing (Earth) - Testing your world without leaving it.</li> <li>Min 25-26: Background Tasks - Native background workers.</li> <li>Min 27-28: Security &amp; Plugins - Signing, hashing and extending.</li> <li>Min 29-30: Mastery &amp; Deployment - The final word and going live.</li> </ul>"},{"location":"#advanced-topics","title":"Advanced Topics","text":"<ul> <li>Plugins System - Extending Heaven with powerful integrations.</li> </ul>"},{"location":"#ready-to-play","title":"Ready to Play?","text":"<pre><code>$ pip install heaven\n</code></pre> <pre><code>from heaven import App\n\n# For instance if this app could talk it might tell you:\n# Hello my name is `your-app` - I am a Python web application\n# I spend my time serving web requests and \n# when I am free - I spend my time dreaming of becoming a chess engine\napp = App()\n\n# The move is yours.\napp.GET('/', lambda req, res, ctx: res.json({'message': 'Checkmate.'}))\n</code></pre>"},{"location":"api/","title":"Heaven API Reference","text":""},{"location":"api/#heavenrouter","title":"<code>heaven.router</code>","text":""},{"location":"api/#router-alias-app","title":"<code>Router</code> (alias <code>App</code>)","text":"<p>The core application class that manages routing, configuration, and lifecycle.</p> <pre><code>class Router(configurator=None, protect_output=True, allow_partials=False, fail_on_output=True, debug=True)\n</code></pre> <p>Parameters: - <code>configurator</code> (Callable | dict, optional): Configuration source. - <code>protect_output</code> (bool): Enable automatic schema validation for responses. Default <code>True</code>. - <code>allow_partials</code> (bool): Allow partial schema validation (non-strict). Default <code>False</code>. - <code>fail_on_output</code> (bool): Raise 500 error on validation failure instead of warning. Default <code>True</code> (Note: Implementation defaults to True, docstring might say otherwise). - <code>debug</code> (bool): Enable debug mode. Default <code>True</code>.</p> <p>Properties: - <code>daemons</code>: (write-only) Register a background task. - <code>earth</code>: (read-only) Lazy-loaded instance of <code>heaven.earth.Earth</code> testing engine. - <code>ws</code>: (read-only) WebSocket status indicator. - <code>_</code>: (read-only) Access to internal buckets via <code>Look</code> interface.</p> <p>Methods: - <code>abettor(method, route, handler, subdomain=DEFAULT, router=None)</code>: Internal method for registering routes. - <code>call(handler, *args, **kwargs)</code>: Execute a handler string (dot-notation) with the app as context. - <code>cors(origins=\"*\", methods=\"*\", headers=\"*\", expose_headers=\"*\", max_age=None, allow_credentials=False)</code>: Enable CORS middleware. - <code>keep(key, value)</code>: Store value in application scope. - <code>listen(host='localhost', port='8701', debug=True, **kwargs)</code>: Start the server using Uvicorn. - <code>mount(router, isolated=True)</code>: Mount another <code>Router</code> instance. <code>isolated</code> determines if configs/buckets are merged. - <code>peek(key)</code>: Retrieve value from application scope. - <code>plugin(plugin_instance)</code>: Register a plugin (must have <code>install(app)</code> method). - <code>sessions(secret_key, cookie_name=\"session\", max_age=3600)</code>: Enable secure cookie sessions. - <code>subdomain(subdomain)</code>: Initialize a new subdomain route engine. - <code>unkeep(key)</code>: Remove and return value from application scope. - <code>websocket()</code>: Enable WebSocket support (flag).</p> <p>Routing Shortcuts: - <code>GET(url, handler)</code> - <code>POST(url, handler)</code> - <code>PUT(url, handler)</code> - <code>DELETE(url, handler)</code> - <code>PATCH(url, handler)</code> - <code>HEAD(url, handler)</code> - <code>OPTIONS(url, handler)</code> - <code>CONNECT(url, handler)</code> - <code>TRACE(url, handler)</code> - <code>SOCKET(url, handler)</code>: Register WebSocket handler. - <code>HTTP(url, handler)</code>: Register handler for all HTTP methods.</p> <p>Hooks: - <code>BEFORE(url, handler)</code>: Pre-request middleware. - <code>AFTER(url, handler)</code>: Post-request middleware. - <code>ON(event, handler)</code>: Lifecycle hooks (<code>'startup'</code>, <code>'shutdown'</code>).</p>"},{"location":"api/#route","title":"<code>Route</code>","text":"<p>Internal node class representing a single route segment or endpoint.</p> <pre><code>class Route(route, handler, router)\n</code></pre> <p>Attributes: - <code>heaven_instance</code>: Reference to the parent <code>Router</code>. - <code>parameterized</code>: Dictionary of parameters at this node. - <code>queryhint</code>: Query string hints. - <code>route</code>: The path segment. - <code>handler</code>: The callable handler (if this is an endpoint). - <code>children</code>: Dictionary of child <code>Route</code> nodes.</p> <p>Methods: - <code>match(routes, r)</code>: Traversing method to find a matching handler for a deque of route segments. - <code>not_found(r, w, c)</code>: Default 404 handler.</p>"},{"location":"api/#routes","title":"<code>Routes</code>","text":"<p>Internal collection class managing the route tree for a specific subdomain.</p> <pre><code>class Routes()\n</code></pre> <p>Attributes: - <code>afters</code>: Dictionary of AFTER hooks. - <code>befores</code>: Dictionary of BEFORE hooks. - <code>cache</code>: Flat cache of registered routes for fast lookup <code>{METHOD: {url: handler}}</code>. - <code>routes</code>: The root nodes of the Radix-like tree.</p> <p>Methods: - <code>add(method, route, handler, router)</code>: Register a route. Handles splitting paths and creating <code>Route</code> nodes. - <code>get_handler(routes)</code>: (Stub) Retrieve handler. - <code>handle(scope, receive, send, metadata, application)</code>: Main ASGI request handling logic. Orchestrates <code>Request</code>, <code>Response</code>, <code>Context</code>, and middleware execution. - <code>remove(method, route)</code>: Unregister a route. - <code>xhooks(hookstore, matched, r, w, c)</code>: Execute hooks for a matched route.</p>"},{"location":"api/#parameter","title":"<code>Parameter</code>","text":"<p>Internal class for handling URL parameters.</p> <pre><code>class Parameter(value, potentials)\n</code></pre> <p>Methods: - <code>resolve(parameter_address)</code>: Resolves the parameter value based on the matched route structure, performing type casting if specified (e.g. <code>:id:int</code>).</p>"},{"location":"api/#schemaregistry","title":"<code>SchemaRegistry</code>","text":"<p>Internal registry for route schemas.</p> <p>Methods: - <code>add(method, route, expects=None, returns=None, ...)</code>: Register schema metadata. - <code>GET(...)</code>, <code>POST(...)</code>, etc.: Shortcuts for <code>add</code>.</p>"},{"location":"api/#heavenrequest","title":"<code>heaven.request</code>","text":""},{"location":"api/#request","title":"<code>Request</code>","text":"<p>Represents an incoming HTTP or WebSocket request.</p> <pre><code>class Request(scope, body, receive, metadata=None, application=None)\n</code></pre> <p>Properties: - <code>app</code>: Parent <code>Router</code> instance. - <code>body</code>: Raw request body (bytes). - <code>cookies</code>: Dictionary of cookies. - <code>data</code>: Validated/Typed request body (if schema present), else alias for <code>json</code>. - <code>form</code>: <code>Form</code> instance (lazy loaded). - <code>headers</code>: Dictionary of headers. - <code>host</code>: Host header value. - <code>ip</code>: Client IP access (<code>req.ip.address</code>). - <code>json</code>: JSON decoded body. - <code>method</code>: HTTP method. - <code>mounted</code>: (Read/Write) Application this request was mounted from. - <code>params</code>: URL path parameters. - <code>qh</code>: (Read/Write) Query hints metadata. - <code>queries</code>: Query string parameters. - <code>querystring</code>: Raw query string. - <code>route</code>: Matched route pattern. - <code>scheme</code>: URL scheme. - <code>server</code>: Server address. - <code>subdomain</code>: Matched subdomain. - <code>url</code>: Request URL path.</p> <p>Methods: - <code>stream()</code>: Async generator for request body (TODO).</p>"},{"location":"api/#form","title":"<code>Form</code>","text":"<p>Handles multipart/form-data and urlencoded parsing.</p> <pre><code>class Form(req)\n</code></pre> <p>Attributes: - <code>_data</code>: Dictionary of parsed data.</p> <p>Methods: - <code>get(name, default=None)</code>: Retrieve a field value. - <code>to_dict()</code>: Return internal dictionary. - <code>__getattr__(name)</code>: Attribute access to fields.</p> <p>Internal Methods: - <code>_parse(req)</code> - <code>_parse_multipart(req, content_type)</code> - <code>_parse_urlencoded(req)</code></p>"},{"location":"api/#heavenresponse","title":"<code>heaven.response</code>","text":""},{"location":"api/#response","title":"<code>Response</code>","text":"<p>Handles sending data back to the client.</p> <pre><code>class Response(app, context, request)\n</code></pre> <p>Properties: - <code>body</code>: Response body (bytes, str, dict, list). - <code>deferred</code>: Boolean indicating if tasks are deferred. - <code>headers</code>: List of headers. - <code>metadata</code>: ASGI response metadata. - <code>status</code>: HTTP status code (int). - <code>template</code>: (Write-only) Template path.</p> <p>Methods: - <code>abort(payload)</code>: Abort execution with payload. - <code>cookie(name, value, **kwargs)</code>: Set cookie. Supports <code>max_age</code>, <code>expires</code>, <code>httponly</code>, <code>samesite</code>, <code>secure</code>, <code>domain</code>, <code>path</code>, <code>partitioned</code>. - <code>defer(func)</code>: Register an async task to run after response is sent. - <code>file(filepath, filename=None, chunk_size=4096)</code>: Stream a file. - <code>header(key, val)</code>: Add a header. - <code>interpolate(name, **contexts)</code>: Async template rendering (returns string). - <code>json()</code>: Decode body as JSON (if body is dict/list, returns it). - <code>out(status, body, headers=None)</code>: Set status, body, and headers at once. - <code>redirect(location, permanent=False)</code>: Send redirect response. - <code>render(name, **contexts)</code>: Async render template to body. - <code>renders(name, **contexts)</code>: Sync render template to body. - <code>stream(generator, content_type='text/plain', status=200, sse=False)</code>: Stream from async generator. - <code>text()</code>: Decode body as string.</p>"},{"location":"api/#methoddispatch","title":"<code>MethodDispatch</code>","text":"<p>Internal decorator class for polymorphism in <code>Response</code> methods (like <code>abort</code>).</p>"},{"location":"api/#heavencontext","title":"<code>heaven.context</code>","text":""},{"location":"api/#context","title":"<code>Context</code>","text":"<p>Request-scoped state container.</p> <pre><code>class Context(application)\n</code></pre> <p>Methods: - <code>keep(key, value)</code>: Store value. - <code>peek(key)</code>: Retrieve value. - <code>unkeep(key)</code>: Remove and return value.</p> <p>Attributes: - Direct attribute access triggers <code>keep</code>/<code>peek</code>. - Reserved keys: <code>session</code>, <code>app</code>, <code>request</code>, <code>response</code>, <code>headers</code>, <code>cookies</code>.</p>"},{"location":"api/#look","title":"<code>Look</code>","text":"<p>Wrapper class enabling dot-notation access for dictionaries (used for <code>ctx.session</code>).</p>"},{"location":"api/#heavenschema","title":"<code>heaven.schema</code>","text":""},{"location":"api/#schema","title":"<code>Schema</code>","text":"<p>Base class for data models. Inherits <code>msgspec.Struct</code>.</p> <pre><code>class Schema(msgspec.Struct)\n</code></pre>"},{"location":"api/#constraints-alias-msgspecmeta","title":"<code>Constraints</code> (alias <code>msgspec.Meta</code>)","text":"<p>Used for field constraints.</p>"},{"location":"api/#heavenearth","title":"<code>heaven.earth</code>","text":""},{"location":"api/#earth","title":"<code>Earth</code>","text":"<p>Testing engine.</p> <pre><code>class Earth(app)\n</code></pre> <p>Methods: - <code>bypass(middleware)</code>: Skip middleware during tests. - <code>context()</code>: Create mock Context. - <code>request(url, ...)</code>: Create mock Request. - <code>response(req=None)</code>: Create mock Response. - <code>swap(old_func, new_func)</code>: Mock dependencies. - <code>test(track_session=True)</code>: Return <code>EarthContextManager</code>. - <code>trio(url, ...)</code>: Return <code>(req, res, ctx)</code> tuple. - <code>upload(url, files=..., data=...)</code>: Simulate multipart upload. - <code>SOCKET(url)</code>: Return <code>MockSocket</code>. - <code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>DELETE</code>, <code>PATCH</code>...: Simulate requests.</p>"},{"location":"api/#earthcontextmanager","title":"<code>EarthContextManager</code>","text":"<p>Context manager for tests. Handles installing/uninstalling hooks and swaps.</p>"},{"location":"api/#mocksocket","title":"<code>MockSocket</code>","text":"<p>Simulator for WebSocket connections.</p> <p>Methods: - <code>connect()</code>: Establish connection. - <code>send(data)</code>: Send data to app. - <code>receive(timeout=None)</code>: Receive data from app. - <code>close(code=1000)</code>: Close connection.</p>"},{"location":"api/#heavenutils-heavenconstants","title":"<code>heaven.utils</code> &amp; <code>heaven.constants</code>","text":""},{"location":"api/#lookup","title":"<code>Lookup</code>","text":"<p>Dictionary wrapper for dot-notation access (used for <code>req.ip</code>).</p>"},{"location":"api/#preprocessor","title":"<code>preprocessor</code>","text":"<p>Internal function to parse ASGI scope and extract subdomain/headers.</p>"},{"location":"api/#constants","title":"Constants","text":"<ul> <li>Methods: <code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>DELETE</code>, <code>PATCH</code>, <code>HEAD</code>, <code>OPTIONS</code>, <code>CONNECT</code>, <code>TRACE</code>, <code>SOCKET</code>.</li> <li>Status: <code>OK</code> (200), <code>CREATED</code> (201), <code>NOT_FOUND</code> (404).</li> <li>Events: <code>STARTUP</code>, <code>SHUTDOWN</code>.</li> <li>Other: <code>DEFAULT</code> ('www'), <code>WILDCARD</code> ('*').</li> </ul>"},{"location":"cli/","title":"The Command Line \ud83d\udee0\ufe0f","text":"<p>In Minute 1, you met <code>heaven fly</code>. But you don't just want to fly, you want to pilot. Heaven's CLI is your cockpit. It is precise, informative, and explicitly typed.</p>"},{"location":"cli/#the-heaven-command","title":"The <code>heaven</code> Command","text":"<p>Type <code>heaven</code> (or <code>heaven -h</code>) to see your controls.</p> <pre><code>$ heaven\nusage: heaven [-h] {fly,run,routes,handlers,schema} ...\n\nHeaven CLI - The divine interface for your web framework.\n\npositional arguments:\n  {fly,run,routes,handlers,schema}\n                        Available commands\n    fly                 Zero-config auto-discovery run\n    run                 Run a specific application\n    routes              Show all registered routes\n    handlers            Deep inspection of handlers\n    schema              Export OpenAPI spec to JSON\n</code></pre>"},{"location":"cli/#1-zero-config-heaven-fly","title":"1. Zero-Config: <code>heaven fly</code>","text":"<p>The <code>fly</code> command is for when you just want to code. It automatically hunts for <code>app.py</code>, <code>main.py</code>, or similar files in your current directory and launches the first <code>App</code> or <code>Router</code> instance it finds.</p> <pre><code># Just fly.\n$ heaven fly\n\n# Fly on a different port.\n$ heaven fly --port 8080 --host 0.0.0.0\n</code></pre> <p>Note</p> <p><code>heaven fly</code> always enables auto-reload. It is designed for development.</p>"},{"location":"cli/#2-explicit-control-heaven-run","title":"2. Explicit Control: <code>heaven run</code>","text":"<p>When you go to production or have a complex project structure, you need <code>run</code>. This command stops the magic guessing game and does exactly what you tell it.</p> <pre><code># Run the 'app' object in 'main.py'\n$ heaven run main:app\n\n# Production mode (no reload, multiple workers)\n# Note: Heaven wraps uvicorn, so for advanced deployment you can use uvicorn directly too.\n$ heaven run main:app --no-reload --host 0.0.0.0 --port 80\n</code></pre>"},{"location":"cli/#3-deep-introspection","title":"3. Deep Introspection","text":"<p>Heaven wants you to know your app better than you know yourself.</p>"},{"location":"cli/#view-your-routes","title":"View Your Routes","text":"<p>See every path, method, subdomain, and protection status in a beautiful table.</p> <pre><code>$ heaven routes\n# or specify the app path explicitly\n$ heaven routes --app main:app\n</code></pre>"},{"location":"cli/#inspect-your-handlers","title":"Inspect Your Handlers","text":"<p>Have you ever forgotten where a specific endpoint is defined? <code>handlers</code> tunnels through decorators, partials, and closures to find the original source code.</p> <pre><code># Interactive map of all handlers and their file locations\n$ heaven handlers\n\n# View the source code of a specific handler right in your terminal\n$ heaven handlers /api/users\n</code></pre>"},{"location":"cli/#export-schema","title":"Export Schema","text":"<p>Need your OpenAPI spec for a CI pipeline or client generator?</p> <pre><code>$ heaven schema\n# outputs to swagger.json by default\n\n$ heaven schema openapi-v1.json\n</code></pre> <p>Next: Now that you can control the server, let's learn how to direct the traffic. On to The Router.</p>"},{"location":"coffee/","title":"Sip some coffee \u2615\ufe0f","text":"<p>You've made it through the core of Heaven! </p> <p>By now, you should feel comfortable with: - Handling Requests - Sending Responses - Managing State via Context - Mounting Apps &amp; Using Daemons</p> <p>Take a moment to enjoy the simplicity. When you're ready, we'll dive into the advanced features and the Full API Reference.</p>"},{"location":"congrats/","title":"Mastery \ud83c\udfc6","text":"<p>Checkmate.</p> <p>You have traversed the board. You know the opening (<code>fly</code>), the mid-game (<code>router</code>, <code>schema</code>), and the endgame (<code>deployment</code>).</p> <p>You are no longer just a Python developer. You are a Heaven Master.</p>"},{"location":"congrats/#the-future","title":"The Future","text":"<p>This documentation is short because the framework is simple. But simplicity does not mean weakness. Heaven is capable of powering everything from microservices to massive monolithic platforms.</p> <p>Go forth and build something divine.</p> <p>\"Simplicity is the ultimate sophistication.\" \u2014 Leonardo da Vinci</p>"},{"location":"congrats/#where-to-go-from-here","title":"Where to go from here?","text":"<ul> <li>API Reference: The raw dictionary of every method.</li> <li>Examples: Real-world patterns and recipes.</li> <li>GitHub: Star the repo, file an issue, or contribute.</li> </ul> <p>May your response times be low and your uptime high. \u26a1</p>"},{"location":"context/","title":"The Context \ud83e\udde0","text":"<p>The <code>Context</code> object is your request-scoped memory. It allows you to pass data between hooks, middlewares, and handlers without cluttering function signatures.</p> <p>The handler signature:</p> <pre><code>async def handler(req, res, ctx):\n    ...\n</code></pre>"},{"location":"context/#usage","title":"Usage","text":""},{"location":"context/#storing-data","title":"Storing Data","text":"<p>You can store data directly on the context using dot notation.</p> <pre><code>app.BEFORE('/dashboard/*', auth_middleware)\n\nasync def auth_middleware(req, res, ctx):\n    user = await db.get_user(req.headers['token'])\n    ctx.user = user\n</code></pre>"},{"location":"context/#retrieving-data","title":"Retrieving Data","text":"<p>Once stored, data is available as a property on the <code>ctx</code> object.</p> <pre><code>async def dashboard_handler(req, res, ctx):\n    # Retrieve 'user' stored by auth_middleware\n    print(f\"Welcome back {ctx.user.name}\")\n</code></pre> <p>Warning</p> <p>You cannot overwrite reserved keys like <code>ctx.session</code>, <code>ctx.request</code>, or <code>ctx.app</code>. Use <code>ctx.keep('session', val)</code> only if you know exactly what you are doing.</p>"},{"location":"context/#why-not-modifies-req","title":"Why not modifies <code>req</code>?","text":"<p>Some frameworks attach data to the <code>Request</code> object. Heaven believes in separation of concerns.</p> <ul> <li>Request: What the client sent (Immutable-ish).</li> <li>Context: What the server figured out (Mutable).</li> <li>Response: What the server is sending (Your Envelope).</li> </ul>"},{"location":"context/#session-management","title":"Session Management","text":"<p>If you enable <code>app.sessions()</code>, the session data lives here.</p> <pre><code># Read\nuser_id = ctx.session.user_id\n\n# Write\nctx.session.visited = True\n</code></pre> <p>Next: We've covered the basics. Now let's superpower your app with Schemas. On to Interceptors.</p>"},{"location":"daemons/","title":"Background Tasks (Daemons)","text":"<p>Heaven includes a powerful, native system for running background tasks, which we call Daemons.</p> <p>Unlike other frameworks that require external message queues (like Celery or Redis) for simple background work, Heaven lets you run persistent background loops directly within your application process.</p>"},{"location":"daemons/#the-appdaemons-api","title":"The <code>app.daemons</code> API","text":"<p>You can register a background task by assigning a function to <code>app.daemons</code>.</p> <pre><code>import asyncio\n\n# A daemon is just a function that takes 'app' as an argument\nasync def heartbeat(app):\n    print(\"Thump-thump...\")\n    # Return the number of seconds to sleep before running again\n    return 5 \n\napp.daemons = heartbeat\n</code></pre>"},{"location":"daemons/#how-it-works","title":"How it works","text":"<ol> <li>Startup: When the application starts, Heaven launches all registered daemons as independent asyncio tasks.</li> <li>Loop: The daemon function is executed.</li> <li>Sleep: If the function returns a number (int/float), Heaven waits that many seconds.</li> <li>Repeat: The function is called again.</li> </ol> <p>If you return <code>None</code> or <code>False</code>, the daemon stops runs once and stops.</p>"},{"location":"daemons/#sync-vs-async","title":"Sync vs Async","text":"<p>Heaven supports both synchronous and asynchronous daemons.</p>"},{"location":"daemons/#async-preferred","title":"Async (Preferred)","text":"<p>Runs in the main event loop. Ideal for I/O bound tasks like checking databases, sending batched emails, or cleaning up caches.</p> <pre><code>async def cleanup_tokens(app):\n    await db.execute(\"DELETE FROM tokens WHERE expired = 1\")\n    return 60 # Run every minute\n</code></pre>"},{"location":"daemons/#sync","title":"Sync","text":"<p>Runs in a separate thread pool executor to avoid blocking the main server loop. Useful for CPU-intensive tasks, though heavily CPU-bound work is still better off in a separate process.</p> <pre><code>def expensive_report(app):\n    data = calculate_heavy_report()\n    save_to_disk(data)\n    return 3600 # Run every hour\n</code></pre>"},{"location":"daemons/#accessing-app-state","title":"Accessing App State","text":"<p>Since daemons receive the <code>app</code> instance, they have full access to your shared state, databases, and plugins.</p> <pre><code>async def mailer(app):\n    # Access the shared database connection\n    queue = await app.db.fetch_pending_emails()\n\n    for email in queue:\n        await send_email(email)\n\n    return 10 # Check every 10 seconds\n</code></pre>"},{"location":"daemons/#event-loop-monitor","title":"Event Loop Monitor","text":"<p>Since Heaven runs on a single thread (asyncio), a single blocking operation can freeze the entire server. Heaven includes a built-in Watchdog to warn you if this happens.</p> <pre><code># Warn if the event loop is blocked for more than 100ms (0.1s)\napp = App(monitor=0.1)\n</code></pre> <p>If a task blocks the loop, you will see a warning in your logs: <code>WARNING:heaven.monitor:Event Loop Blocked! Lag: 0.1504s</code>. This is invaluable for debugging latency spikes.</p> <p>Next: It works locally. Let's show the world. On to Deployment.</p>"},{"location":"deployment/","title":"Deployment \ud83d\ude80","text":"<p>You have built a masterpiece. Now it must scale.</p> <p>Heaven is built on top of ASGI (Asynchronous Server Gateway Interface), which means it can be served by any industry-standard ASGI server like <code>uvicorn</code>, <code>hypercorn</code>, or <code>daphne</code>.</p>"},{"location":"deployment/#method-1-the-heaven-way","title":"Method 1: The Heaven Way","text":"<p>The simplest way to deploy is using the built-in CLI, which wraps <code>uvicorn</code> with optimal defaults.</p> <pre><code>$ heaven run main:app --host 0.0.0.0 --port 80 --no-reload --workers 4\n</code></pre> <ul> <li><code>--no-reload</code>: Vital for production performance.</li> <li><code>--workers 4</code>: Run multiple processes to utilize all CPU cores.</li> </ul>"},{"location":"deployment/#method-2-gunicorn-the-pro-way","title":"Method 2: Gunicorn (The Pro Way)","text":"<p>For robust process management, <code>gunicorn</code> with <code>uvicorn</code> workers is the industry standard.</p> <pre><code>$ pip install gunicorn\n$ gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:80\n</code></pre>"},{"location":"deployment/#method-3-docker","title":"Method 3: Docker","text":"<p>Keep it contained.</p> <pre><code>FROM python:3.9-slim\n\nWORKDIR /app\nCOPY . .\nRUN pip install heaven\n\n# Run via Heaven CLI\nCMD [\"heaven\", \"run\", \"main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"80\", \"--no-reload\"]\n</code></pre>"},{"location":"deployment/#reverse-proxy","title":"Reverse Proxy","text":"<p>Always put Nginx or Caddy in front of your Heaven app to handle SSL, static assets, and load balancing.</p> <p>Next: You made it. On to Mastery.</p>"},{"location":"earth/","title":"The Earth \ud83c\udf0d","text":"<p>Testing web apps usually involves spinning up a \"test client\", fiddling with ports, or mocking complex internal states. Heaven gives you <code>Earth</code>, a testing utility that lets you verify your world without leaving Python.</p>"},{"location":"earth/#the-testing-philosophy","title":"The Testing Philosophy","text":"<p>Heaven tests are:</p> <ol> <li>In-Process: No network overhead.</li> <li>Explicit: You see exactly what <code>req</code>, <code>res</code>, and <code>ctx</code> look like.</li> <li>Flexible: Test full lifecycles, atomic functions, or swapped configurations.</li> </ol>"},{"location":"earth/#1-full-integration-tests","title":"1. Full Integration Tests","text":"<p>Use the <code>test()</code> context manager to simulate a real server environment (including startup/shutdown hooks).</p> <pre><code># test_app.py\nfrom main import app\n\nasync def test_create_user():\n    # track_session=True automatically handles Cookies across requests\n    async with app.earth.test(track_session=True) as earth:\n        # 1. Send Request\n        req, res, ctx = await earth.POST('/users', body={'name': 'Ray'})\n\n        # 2. Assert Response\n        assert res.status == 201\n        assert res.json['name'] == 'Ray'\n</code></pre>"},{"location":"earth/#2-unit-testing-handlers","title":"2. Unit Testing Handlers","text":"<p>Sometimes you just want to test a single function without running the whole router logic.</p> <pre><code>from main import create_user_handler\n\nasync def test_handler_logic():\n    # 1. Create fake components\n    req = app.earth.req(url='/', body={'name': 'Ray'})\n    res = app.earth.res()\n    ctx = app.earth.ctx()\n\n    # 2. Call handler directly\n    await create_user_handler(req, res, ctx)\n\n    # 3. Verify\n    assert res.status == 201\n</code></pre>"},{"location":"earth/#3-mocking-swapping-bypassing","title":"3. Mocking, Swapping &amp; Bypassing","text":"<p>You often need to mock databases, avoid rate limits, or swap authentication logic.</p>"},{"location":"earth/#hook-swapping","title":"Hook Swapping","text":"<p>Swap out a startup hook (like <code>connect_db</code>) with a mock version.</p> <pre><code># The real startup hook\nasync def connect_prod_db(app): ...\n\n# The test startup hook\nasync def connect_test_db(app): ...\n\n# Swap them\napp.earth.swap(connect_prod_db, connect_test_db)\n</code></pre>"},{"location":"earth/#middleware-bypassing","title":"Middleware Bypassing","text":"<p>Skip specific middleware (like Rate Limiters) that might interfere with tests.</p> <pre><code># Don't run this middleware during tests\napp.earth.bypass(rate_limiter_hook)\n</code></pre>"},{"location":"earth/#bucket-mocking","title":"Bucket Mocking","text":"<p>If your app uses <code>app.peek('db')</code>, you can overwrite it for the test.</p> <p>Warning</p> <p><code>app.keep</code> is persistent across tests! If you overwrite a global dependency, you must manually restore it, otherwise downstream tests will use your mock.</p> <pre><code>async with app.earth.test() as earth:\n    # 1. Backup original\n    original_db = app.unkeep('db')\n\n    # 2. Overwrite with mock\n    app.keep('db', MockDatabase())\n\n    try:\n        await earth.GET('/users')\n    finally:\n        # 3. Restore original for other tests\n        app.keep('db', original_db)\n</code></pre>"},{"location":"earth/#4-subdomains-websockets-file-uploads","title":"4. Subdomains, WebSockets &amp; File Uploads","text":"<p>Earth handles detailed scenarios easily.</p> <pre><code># Test a subdomain\nawait earth.GET('/admin', subdomain='admin')\n\n# Test a WebSocket\nws = await earth.SOCKET('/chat').connect()\nawait ws.send('hello')\nassert await ws.receive() == 'world'\nawait ws.close()\n\n# Test File Uploads\nawait earth.upload('/avatar', \n    files={'file': ('image.png', b'data')},\n    data={'userid': '123'}\n)\n</code></pre> <p>Next: Confident much? Let your app run in the background with Daemons.</p>"},{"location":"examples/","title":"Appendix&colon; Examples","text":""},{"location":"examples/#how-tos-and-code-examples","title":"How-To's and Code Examples","text":"<p>Heaven is currently being used in production by a few customer facing web products and to power high performance microservices. Below are some of the observations and collated practices that might help provide some guidance on code patterns possible in heaven.</p>"},{"location":"examples/#decorator-functions","title":"Decorator Functions","text":"<p>Useful for intercepting and performing actions before the decorated function is invoked i.e. <code>authentication</code>, <code>dependency injection</code>, or <code>authorization</code> amongst other possibilities.</p> <p>This example demonstrates one of the ways Authentication might be implemented with the <code>pyjwt</code> library. <pre><code>from asyncpg import Pool\nfrom jwt import decode\n\n\ndef protected(func):\n    \"\"\"Authentication\"\"\"\n    @wraps(func)\n    def delegate(r: Request, w: Response, c: Context):\n        token = r.headers.get('authorization')\n        secret_key = r.app.CONFIG('SECRET_KEY')\n\n        try: credentials = decode(token, secret_key, algorithm='HS256')\n        except: return w.status = status.UNAUTHORIZED\n        else: c.keep('current_user') = credentials.get('id')\n\n        return await func(r, w, c)\n    return delegate\n\n\ndef private(resource: str):\n    \"\"\"RBAC Authorization, heaven makes it easy to also use ABAC\"\"\"\n    def wrapper(func):\n        @wraps(func)\n        def delegate(r: Request, w: Response, c: Context):\n            dbpool: Pool = r.app.peek('dbpool')\n            action = r.method.lower()\n            with dbpool.acquire() as sqld:\n                try: roles = await sqld.fetchval(\"\"\"\n                    SELECT roles\n                    FROM privileges\n                    WHERE user = $1 AND action = $2 AND resource = $3\n                \"\"\", c.current_user, action, resource)\n                except:\n                    w.status = HTTPStatus.SERVICE_UNAVAILABLE\n                    w.body = dumps({'message': 'please try again later'})\n            if action in roles: return await func(r, w, c)\n\n            w.status = HTTPStatus.UNAUTHORIZED\n            w.body = dumps({'message': 'insufficient privileges'})\n        return delegate\n    return wrapper\n</code></pre></p>"},{"location":"examples/#schema-validation-documentation","title":"Schema Validation &amp; Documentation","text":"<p>Use <code>router.schema</code> to define your API contracts and <code>router.DOCS</code> to generate interactive documentation.</p> <pre><code>from heaven import App, Schema\n\napp = App()\n\nclass User(Schema):\n    id: int\n    email: str\n    role: str = \"guest\"\n\n# Define your handler\nasync def create_user(req, res, ctx):\n    user = req.data # Validated User struct\n    print(f\"Creating user: {user.email}\")\n    res.body = {\"status\": \"created\", \"id\": 123}\n\n# 1. Register endpoint metadata\napp.schema.POST(\n    '/v1/users', \n    expects=User, \n    returns=dict, \n    summary=\"Create a new user\",\n    description=\"Creates a guest user by default if role is not provided.\"\n)\n\n# 2. Register handler\napp.POST('/v1/users', create_user)\n\n# 3. Enable Scalar Documentation\napp.DOCS('/api/docs', title=\"User Service API\")\n</code></pre> <p>The above setup provides: - Automatic 422 errors for invalid JSON. - Static typing in your handler via <code>req.data</code>. - Interactive docs at <code>/api/docs</code>.</p>"},{"location":"hooks/","title":"Interceptors (Hooks &amp; Middlewares)","text":"<p>Heaven uses a centralized <code>interceptors</code> system to handle middleware. Instead of \"wrapping\" applications in layers of opaque middleware, you register interceptors i.e. <code>hooks</code> that run <code>.BEFORE</code> or <code>.AFTER</code> a request.</p>"},{"location":"hooks/#the-hook-lifecycle","title":"The Hook Lifecycle","text":"<ol> <li>Request arrives.</li> <li>Match Route: The router finds the handler for the URL.</li> <li>Run BEFORE Hooks: Any hooks matching the URL are run.</li> <li>Run Handler: The actual route handler is executed.</li> <li>Run AFTER Hooks: Any hooks matching the URL are run.</li> <li>Response sent.</li> </ol>"},{"location":"hooks/#registering-hooks","title":"Registering Hooks","text":"<p>Note</p> <p>Hooks are run in the order they are registered.</p> <p>Hooks are registered with a glob-like URL pattern.</p> <pre><code># Run on EVERY request\napp.BEFORE('/*', global_auth_check)  # run me 1st\n\n# Run only on /api routes\napp.BEFORE('/api/*', rate_limiter)  # run me 2nd\n\n# Run on a specific route\napp.AFTER('/users/:id', log_access)  # run me 3rd\n</code></pre>"},{"location":"hooks/#execution-order-mounting","title":"Execution Order &amp; Mounting","text":"<ol> <li>FIFO: Hooks are executed in the exact order they are registered.</li> <li>Mounting: When you mount a sub-application (<code>child</code>) onto a <code>parent</code> app, the order is preserved nicely:<ul> <li>BEFORE Hooks: Parent hooks run first, then Child hooks. (Guard Pattern: Global Auth -&gt; Specific Logic).</li> <li>AFTER Hooks: Child hooks run first, then Parent hooks. (Unwinding: Specific Cleanup -&gt; Global Cleanup).</li> </ul> </li> </ol>"},{"location":"hooks/#the-hook-signature","title":"The Hook Signature","text":"<p>Hooks share the exact same signature as route handlers.</p> <pre><code>async def my_hook(req, res, ctx):\n    # You can modify the request\n    req.headers['x-checked'] = 'true'\n\n    # You can modify the context\n    ctx.user = await get_user(req)\n\n    # You can ABORT the request (shortcuts the pipeline)\n    if not ctx.user:\n        res.status = 401\n        res.abort(\"Unauthorized\")\n</code></pre>"},{"location":"hooks/#cors-cross-origin-resource-sharing","title":"CORS (Cross-Origin Resource Sharing)","text":"<p>CORS is a common requirement, so Heaven includes a dedicated helper that registers the necessary hooks for you.</p>"},{"location":"hooks/#simple","title":"Simple","text":"<pre><code># Allow everything (wildcard)\napp.cors() \n</code></pre>"},{"location":"hooks/#configured","title":"configured","text":"<pre><code>app.cors(\n    origin='https://myapp.com',\n    methods=['GET', 'POST'],\n    headers=['Authorization', 'Content-Type'],\n    max_age=3600 # or maxAge, MAX_AGE etc.\n)\n</code></pre> <p>Note</p> <p><code>app.cors</code> is smart enough to handle different casing styles for arguments (e.g. <code>max_age</code>, <code>maxAge</code>, <code>MAX_AGE</code> all work).</p> <p>Note that <code>app.cors()</code> is just a wrapper that registers a <code>.BEFORE('/*')</code> hook that handles the <code>OPTIONS</code> method and sets the correct <code>Access-Control-*</code> headers.</p> <p>Next: You know how to intercept, how about API Docs? On to Schemas.</p>"},{"location":"html/","title":"Templating &amp; Files","text":"<p>Heaven uses Jinja2 for templating, but keeps it async and non-blocking.</p>"},{"location":"html/#serving-static-files","title":"Serving Static Files","text":"<p>First, let's serve your CSS and images.</p> <pre><code># Map /static to 'assets' folder\n# 'OR' serve everything in 'assets' folder at '/static' URL\napp.ASSETS('/static', 'assets')\n</code></pre> <p>Now <code>assets/logo.png</code> is available at <code>http://localhost:8000/static/logo.png</code>.</p>"},{"location":"html/#enabling-templates","title":"Enabling Templates","text":"<p>Tell Heaven where your templates live.</p> <pre><code># 'templates' is the folder name\napp.TEMPLATES('templates')\n</code></pre>"},{"location":"html/#rendering-html","title":"Rendering HTML","text":"<p>You can render templates asynchronously.</p> <pre><code>async def index(req, res, ctx):\n    ctx.keep('user_name', 'Visitor')\n\n    # Render 'index.html'\n    # You can pass extra variables directly (e.g. title)\n    await res.render('index.html', title=\"Welcome Home\")\n</code></pre>"},{"location":"html/#automatic-context","title":"Automatic Context","text":"<p>Heaven automatically injects the three musketeer objects into every template:</p> <ul> <li><code>req</code>: The current Request object <code>a.k.a</code> Portos.</li> <li><code>res</code>: The current Response object <code>a.k.a</code> Athos.</li> <li><code>ctx</code>: The current Context object <code>a.k.a</code> Aramis.</li> </ul> <p>PS: D'Artagnan is not a musketeer but in the spirit of camarederie he can be the Router</p> <p>In your <code>index.html</code>:</p> <pre><code>&lt;!-- Access variables from ctx --&gt;\n&lt;h1&gt;Hello, {{ ctx.user_name }}&lt;/h1&gt;\n\n&lt;!-- Access request info --&gt;\n&lt;p&gt;You are visiting: {{ req.url }}&lt;/p&gt;\n\n&lt;!-- Access response info --&gt;\n&lt;p&gt;Status: {{ res.status }}&lt;/p&gt;\n\n&lt;!-- Access passed variables --&gt;\n&lt;title&gt;{{ title }}&lt;/title&gt;\n</code></pre> <p>Next: We've covered the basics. Now let's superpower your app with Schemas. On to Interceptors.</p>"},{"location":"marketplace/","title":"Plugins Marketplace","text":"<p>Welcome to the Heaven Plugin Marketplace. Here you will find \"Official Recipes\" \u2014 plugins that are essential for many applications but are kept out of the core to ensure the framework remains lightweight and fast.</p> <p>You can simply copy and paste these into your project (e.g., in a <code>plugins.py</code> file).</p>"},{"location":"marketplace/#official-recipes","title":"Official Recipes","text":"<p>Note</p> <p>CORS and Sessions are now native core features! Check out The Router documentation for details.</p>"},{"location":"marketplace/#heaven-plugins","title":"Heaven Plugins","text":"Plugin Pypi Description Source Code SecurityPlugin heaven_security Add authentication and authorization to your API. Source Code"},{"location":"mount/","title":"Mounting Routers","text":"<p>Heaven allows for applications to be mounted on top of each other. See example below</p> <p>First let's create our backend api as a heaven application/router in a file called:</p> <p><code>api.py</code> <pre><code>from heaven import Router\n\napi = Router()\napi.GET('/v1/customers', lambda req, res, ctx:...)\n</code></pre></p> <p>Next we create our frontend renderer as another heaven application/router:</p> <p><code>pages.py</code> <pre><code>from heaven import Router\n\npages = Router()\n\n# folder where your templates are stored\npages.TEMPLATES('templates', asynchronous=False)\npages.ASSETS('assets')\n\n\npages.GET('/', lambda req, res, ctx: res.renders('index.html'))\n</code></pre></p> <p>Finally we create our main heaven application that will configure database connections and mount the <code>backend app</code> and <code>frontend app</code> on itself as mounted children:</p> <p><code>app.py</code> <pre><code>from heaven import Application\n\napp = Application()\n\napp.mount(api, isolated=True)\napp.mount(pages, isolated=False)\n</code></pre> isolation=False means no middlewares, config, or state is shared between router instances - only routes will be mounted.</p>"},{"location":"openapi/","title":"Auto-Generated API Docs (OpenAPI)","text":"<p>Heaven automatically generates an OpenAPI 3.0 (Swagger) specification for your API, derived directly from your code and schemas.</p>"},{"location":"openapi/#enabling-documentation","title":"Enabling Documentation","text":"<p>The documentation UI is not enabled by default to keep things lightweight. You enable it by mounting the docs endpoint.</p> <pre><code># Serves Swagger UI at /docs and schema at /docs/openapi.json\napp.DOCS(route='/docs', title=\"My API\", version=\"1.0.0\")\n</code></pre>"},{"location":"openapi/#connecting-schemas","title":"Connecting Schemas","text":"<p>The documentation power comes from the <code>SchemaRegistry</code>. When you register a route with <code>.schema</code>, Heaven learns what the endpoint expects and returns.</p> <pre><code>class CreateItem(Schema):\n    name: str\n    price: float\n\nclass ItemResponse(Schema):\n    id: int\n    name: str\n\napp.schema.POST(\n    '/items',\n    expects=CreateItem,\n    returns=ItemResponse,\n    summary=\"Create a new item\",\n    description=\"Adds a new item to the inventory and returns it with an ID.\",\n    group=\"Inventory\" # Groups endpoints in the UI\n)\n</code></pre>"},{"location":"openapi/#grouping","title":"Grouping","text":"<p>Heaven automatically groups endpoints based on the URL structure (e.g. <code>/users</code> and <code>/users/:id</code> are grouped under \"Users\").</p> <p>You can override this with the <code>group</code> parameter:</p> <pre><code>app.schema.GET('/system/health', group=\"Monitoring\", ...)\n</code></pre>"},{"location":"openapi/#subdomain-documentation","title":"Subdomain Documentation","text":"<p>If you are using subdomains, you can enable documentation specifically for that subdomain.</p> <pre><code>api = app.subdomain('api')\n# This documentation will ONLY show routes registered to the 'api' subdomain\napi.doc('/docs', title=\"Public API\")\n</code></pre> <p>Next: You must have written a lot of code by now, but does it work? Let's see on Earth.</p>"},{"location":"plugins/","title":"Plugins","text":"<p>Heaven provides a simple yet powerful plugin system that allows you to extend the framework's functionality, integrate with databases, or add custom behavior.</p>"},{"location":"plugins/#the-plugin-protocol","title":"The Plugin Protocol","text":"<p>A plugin is simply a class (or object) that implements an <code>install</code> method. This method receives the application instance (<code>Router</code>) as its only argument, allowing you to register hooks, inject context, or modify configuration.</p> <pre><code>class MyPlugin:\n    def install(self, app):\n        # Register hooks\n        app.ON('startup', self.connect)\n\n        # Inject into context\n        app.keep('my_plugin', self)\n\n    async def connect(self, app):\n        print(\"Plugin connected!\")\n</code></pre>"},{"location":"plugins/#using-plugins","title":"Using Plugins","text":"<p>Registering a plugin is done via <code>app.plugin()</code>:</p> <pre><code>app = Router()\napp.plugin(MyPlugin())\n</code></pre>"},{"location":"plugins/#plugin-marketplace","title":"Plugin Marketplace","text":"<p>Looking for ready-made plugins? Check out the Plugin Marketplace for community plugins and integrations.</p>"},{"location":"plugins/#advanced-example-pg_heaven","title":"Advanced Example: <code>pg_heaven</code>","text":"<p>Here is a complete example of a PostgreSQL plugin that integrates seamless <code>msgspec</code> destructuring for high-performance data retrieval.</p>"},{"location":"plugins/#the-plugin","title":"The Plugin","text":"<pre><code>import msgspec\nimport asyncpg\nfrom typing import Type, TypeVar, List, Any\n\nT = TypeVar(\"T\")\n\nclass PostgresPlugin:\n    def __init__(self, dsn: str, name: str = \"pg\"):\n        self.dsn = dsn\n        self.name = name\n        self.pool = None\n\n    def install(self, app):\n        \"\"\"The distinct integration point.\"\"\"\n\n        # 1. Register Lifecycle Hooks\n        # Heaven automatically manages the pool connection/disconnection\n        app.ON(\"startup\", self.startup)\n        app.ON(\"shutdown\", self.shutdown)\n\n        # 2. Inject into Context\n        # Using a BEFORE hook ensures 'ctx.pg' is available on every request\n        # with zero overhead for the end-user\n        async def inject_db(req, res, ctx):\n            # This makes 'ctx.db' or 'ctx.pg' available\n            ctx.keep(self.name, self)\n\n        app.BEFORE(\"/*\", inject_db)\n\n    async def startup(self, app):\n        self.pool = await asyncpg.create_pool(self.dsn)\n        print(f\"\ud83d\udd0c {self.name} connected\")\n\n    async def shutdown(self, app):\n        await self.pool.close()\n        print(f\"\ud83d\udd0c {self.name} disconnected\")\n\n    # The Magic Method: Msgspec Integration\n    async def fetch(self, query: str, *args, bind: Type[T] = None) -&gt; List[T] | List[Any]:\n        \"\"\"\n        Executes query and seamlessly destructures into a msgspec Struct \n        if 'bind' is provided.\n        \"\"\"\n        rows = await self.pool.fetch(query, *args)\n\n        if bind:\n            # fast-path: asyncpg Record -&gt; dict -&gt; msgspec Struct\n            # msgspec.convert is extremely fast at this\n            return msgspec.convert(rows, List[bind])\n\n        return rows\n</code></pre>"},{"location":"plugins/#usage-in-application","title":"Usage in Application","text":"<p>This setup allows for implicit context availability and explicit, typed destructuring.</p> <pre><code>from heaven import Router\nimport msgspec\n\n# 1. Define your data structure\nclass User(msgspec.Struct):\n    id: int\n    email: str\n    is_active: bool\n\napp = Router()\n\n# 2. Register (once)\napp.plugin(PostgresPlugin(dsn=\"postgres://user:pass@localhost/db\", name=\"db\"))\n\n# 3. Use in handler\nasync def get_users(req, res, ctx):\n    # 'ctx.db' is auto-injected\n    # 'bind=User' automatically converts the SQL result to List[User]\n    users = await ctx.db.fetch(\"SELECT * FROM users\", bind=User)\n\n    # 'users' is now a list of Structs, ready for validation or automatic encoding\n    res.body = users\n\napp.GET(\"/users\", get_users)\n</code></pre>"},{"location":"production/","title":"Going to Production \ud83d\ude80","text":"<p>You've built your application. It runs locally. Now let's make it bulletproof for the real world.</p>"},{"location":"production/#security-headers","title":"Security Headers \ud83d\udee1\ufe0f","text":"<p>By default, Heaven does not set opinionated security headers. In production, you absolutely should.</p> <p>Copy this snippet into your application:</p> <pre><code>async def security_headers(req, res, ctx):\n    # Prevent MIME-Sniffing\n    res.headers = 'X-Content-Type-Options', 'nosniff'\n\n    # Clickjacking Protection (Same Origin Only)\n    res.headers = 'X-Frame-Options', 'SAMEORIGIN'\n\n    # Cross-Site Scripting Protection (Legacy Browsers)\n    res.headers = 'X-XSS-Protection', '1; mode=block'\n\n    # HTTP Strict Transport Security (HSTS) - Enforce HTTPS\n    # Max-age: 1 year (31536000 seconds)\n    # includeSubDomains: Apply to all subdomains\n    # preload: Allow inclusion in browser HSTS preload list\n    res.headers = 'Strict-Transport-Security', 'max-age=31536000; includeSubDomains; preload'\n\n    # Referrer Policy - Don't leak paths to other sites\n    res.headers = 'Referrer-Policy', 'strict-origin-when-cross-origin'\n\n# Apply globally\napp.AFTER('/*', security_headers)\n\n# OR\n\napp.AFTER('/*', 'path.to.security_headers')\n</code></pre>"},{"location":"production/#content-security-policy-csp","title":"Content Security Policy (CSP) \ud83d\udc6e","text":"<p>CSP is the heavy artillery of web security. It prevents script injection (XSS) by whitelisting exactly where content can come from.</p> <p>Warning</p> <p>A strict CSP can break your app if you use inline scripts or external CDNs not whitelisted. Test thoroughly!</p>"},{"location":"production/#strict-default-example","title":"Strict Default Example","text":"<p>This policy allows scripts/styles only from your own domain (<code>'self'</code>).</p> <pre><code>async def csp_header(req, res, ctx):\n    policy = (\n        \"default-src 'self'; \"\n        \"script-src 'self'; \"\n        \"style-src 'self'; \"\n        \"img-src 'self' data:; \" # Allow images from self and base64 data URIs\n        \"font-src 'self'; \"\n        \"object-src 'none'; \"     # Block Flash/Plugins\n        \"base-uri 'self'; \"\n        \"form-action 'self'; \"\n        \"frame-ancestors 'none';\" # Prevent embedding (Clickjacking)\n    )\n    res.headers = 'Content-Security-Policy', policy\n\napp.AFTER('/*', csp_header)\n</code></pre>"},{"location":"production/#secrets-sessions","title":"Secrets &amp; Sessions \ud83d\udd11","text":"<p>Never hardcode secrets in your code. Use Environment Variables.</p> <pre><code>import os\nfrom heaven import Router\n\n# Load from environment (or .env file using python-dotenv)\nSECRET_KEY = os.getenv('SECRET_KEY')\n\nif not SECRET_KEY:\n    raise ValueError(\"No SECRET_KEY set for production application\")\n\napp = Router()\n\n# Enable sessions with the secure key\napp.sessions(secret_key=SECRET_KEY, max_age=86400, cookie_name='__Secure-Session')\n</code></pre>"},{"location":"production/#deployment","title":"Deployment \ud83c\udfed","text":""},{"location":"production/#running-with-gunicorn-process-manager","title":"Running with Gunicorn (Process Manager)","text":"<p>While <code>python main.py</code> uses Uvicorn, in production you should use Gunicorn to manage multiple worker processes.</p> <p>Install Gunicorn: <pre><code>pip install gunicorn\n</code></pre></p> <p>Run Command: <pre><code># -w 4: Run 4 worker processes\n# -k uvicorn.workers.UvicornWorker: Use Uvicorn for async\n# main:app : The file `main.py` and the variable `app`\ngunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app\n</code></pre></p>"},{"location":"production/#nginx-configuration-reverse-proxy","title":"Nginx Configuration (Reverse Proxy)","text":"<p>Put Nginx in front of Gunicorn to handle SSL termination and static files.</p> <pre><code>server {\n    listen 80;\n    server_name example.com;\n\n    # Redirect HTTP to HTTPS\n    return 301 https://$host$request_uri;\n}\n\nserver {\n    listen 443 ssl http2;\n    server_name example.com;\n\n    ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;\n\n    # Serve Static Assets directly\n    # This bypasses Heaven for maximum performance\n    location /public {\n        alias /var/www/my-app/public;\n        expires 30d;\n        access_log off;\n    }\n\n    location / {\n        proxy_pass http://127.0.0.1:8000;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n\n        # Websocket Support\n        proxy_http_version 1.1;\n        proxy_set_header Upgrade $http_upgrade;\n        proxy_set_header Connection \"upgrade\";\n    }\n}\n</code></pre>"},{"location":"quickstart/","title":"Minute 1: The Beginning \u26a1","text":"<p>The clock is ticking. 60 seconds to a running server.</p>"},{"location":"quickstart/#installation","title":"Installation","text":"<pre><code>$ pip install heaven\n</code></pre>"},{"location":"quickstart/#the-first-move","title":"The First Move","text":"<p>Create a file named <code>app.py</code>:</p> <pre><code>from heaven import App\n\napp = App()\n\n# Handlers receive 3 arguments: Request, Response, Context\nasync def hello(req, res, ctx):\n    res.body = \"Hello from Heaven\"\n\n# Map the URL '/' to the 'hello' handler\napp.GET('/', hello)\n</code></pre> <p>Pro Tip: Developer Speed</p> <p>As your application grows, importing hundreds of handlers at the top of your file becomes tedious. Heaven lets you pass the import path as a string.</p> <pre><code># Heaven lazy-loads this module only when needed! \n# No more giant import lists.\napp.GET('/profile', 'handlers.users.get_profile')\n</code></pre>"},{"location":"quickstart/#lift-off","title":"Lift Off","text":"<p>In your terminal, run:</p> <pre><code>$ heaven fly\n</code></pre> <p>OR</p> <pre><code>$ heaven run app:app\n</code></pre> <p>You should see: <pre><code>INFO:     Started server process [1234]\nINFO:     Waiting for application startup.\nINFO:     Application startup complete.\nINFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)\n</code></pre></p> <p>Open your browser to <code>http://localhost:8000</code>.  Congratulations. Only 28 minutes left.</p> <p>Next: You're flying, but now let's take control. On to The Command Line.</p>"},{"location":"reference/","title":"Reference","text":""},{"location":"reference/#alphabetical-list-of-all-heaven-apis","title":"Alphabetical List of All Heaven APIs","text":"<ul> <li> <p><code>constants.DEFAULT</code> -&gt; A <code>str</code> constant helpful to preven you from typing <code>'www'</code> everywhere in your code.</p> </li> <li> <p><code>constants.WILDCARD</code> -&gt; A <code>str</code> constant helpful to prevent you from typing <code>'*'</code> everywhere in your code.</p> </li> <li> <p><code>context.keep(key: str, value: any)</code> -&gt; This is how values are kept/stored in the context API for use across http requests.     see decorator code snippet for example on its usage</p> </li> <li> <p><code>form.Form</code> -&gt; Tiny wrapper around a dict returned from <code>req.FORM</code> when content-type of request is of type <code>multitype/form-data</code> or     <code>application/x-www-form-urlencoded</code> <pre><code>from heaven.form import Form\n\nHEADERS = ['multitype/form-data', 'application/x-www-form-urlencoded']\n\nasync def example(req, res, ctx):\n    if(req.headers.get('content-type') in HEADERS):\n        isinstance(req.form, Form)  # --&gt; True\n        print(form.email)\n        print(form.password)\n    else:\n        assert req.form is None  # --&gt; True\n</code></pre></p> </li> <li> <p><code>router.AFTER(url: str, handler: func, subdomain: str)</code> -&gt; This is called a hook - a function that is hooked to run after all matching routes.     <pre><code>message = 'I will run after all /v1/* routes'\nrouter.AFTER('/v1/*', lambda req, res, ctx: print(message))\n\n# will run after\nrouter.GET('/v1/customers', ...)\nrouter.POST('/v1/leads', ...)\n\n# but not after\nrouter.GET('/v2/customers')\n</code></pre></p> </li> <li> <p><code>router.BEFORE(url: str, handler: func, subdomain: str)</code> -&gt; Same as after hook above - but runs before all matching routes.</p> </li> <li> <p><code>router.CONNECT(url: str, handler: func, subdomain: str)</code> -&gt; Registers your custom handlers/functions to be invoked when a <code>CONNECT</code> http     request to the provided <code>url</code> is received.</p> </li> <li> <p><code>router.DELETE(url: str, handler: func, subdomain: str)</code> -&gt; Registers your custom handlers/functions to be invoked when a <code>DELETE</code> http     request to the provided <code>url</code> is received.</p> </li> <li> <p><code>router.GET(url: str, handler: func, subdomain: str)</code> -&gt; Registers your custom handlers/functions to be invoked when a <code>GET</code> http     request to the provided <code>url</code> is received.</p> </li> <li> <p><code>router.HEAD(url: str, handler: func, subdomain: str)</code> -&gt; Registers your custom handlers/functions to be invoked when a <code>HEAD</code> http     request to the provided <code>url</code> is received.</p> </li> <li> <p><code>router.HTTP(url: str, handler: func, subdomain: str)</code> -&gt; Registers the same <code>handler</code> <code>func: Callable[[Request, Response, Context], None]</code>      to be called for <code>ALL</code> http request methods i.e. GET, PUT, POST, PUT, PATCH etc. instead of doing it individually.     <pre><code># this is one line\nrouter.HTTP('/', lambda req, res, ctx: res.renders('index.html'))\n\n# but is the same as this\nrouter.CONNECT('/', lambda req, res, ctx: res.renders('index.html'))\nrouter.DELETE('/', lambda req, res, ctx: res.renders('index.html'))\nrouter.GET('/', lambda req, res, ctx: res.renders('index.html'))\nrouter.HEAD('/', lambda req, res, ctx: res.renders('index.html'))\nrouter.OPTIONS('/', lambda req, res, ctx: res.renders('index.html'))\nrouter.PATCH('/', lambda req, res, ctx: res.renders('index.html'))\nrouter.POST('/', lambda req, res, ctx: res.renders('index.html'))\nrouter.PUT('/', lambda req, res, ctx: res.renders('index.html'))\nrouter.TRACE('/', lambda req, res, ctx: res.renders('index.html'))\n</code></pre></p> </li> <li> <p><code>router.OPTIONS(url: str, handler: func, subdomain: str)</code> -&gt; Registers your custom handlers/functions to be invoked when a <code>OPTIONS</code> http     request to the provided <code>url</code> is received.</p> </li> <li> <p><code>router.PATCH(url: str, handler: func, subdomain: str)</code> -&gt; Registers your custom handlers/functions to be invoked when a <code>PATCH</code> http     request to the provided <code>url</code> is received.</p> </li> <li> <p><code>router.POST(url: str, handler: func, subdomain: str)</code> -&gt; Registers your custom handlers/functions to be invoked when a <code>POST</code> http     request to the provided <code>url</code> is received.</p> </li> <li> <p><code>router.PUT(url: str, handler: func, subdomain: str)</code> -&gt; Registers your custom handlers/functions to be invoked when a <code>PUT</code> http     request to the provided <code>url</code> is received.</p> </li> <li> <p><code>router.TRACE(url: str, handler: func, subdomain: str)</code> -&gt; Registers your custom handlers/functions to be invoked when a <code>TRACE</code> http     request to the provided <code>url</code> is received.</p> </li> </ul>"},{"location":"request/","title":"The Request \ud83d\udce8","text":"<p>You've built the airport (Router), but here comes the plane. The <code>Request</code> object contains everything the client sent you, pre-parsed and ready to fly.</p> <p>The signature of every handler in Heaven is:</p> <pre><code>async def handler(req, res, ctx):\n    ...\n</code></pre> <p>Let's dissect <code>req</code>.</p>"},{"location":"request/#the-basics","title":"The Basics","text":"<ul> <li><code>req.body</code>: (bytes) The raw body.</li> <li><code>req.method</code>: (str) <code>GET</code>, <code>POST</code>, etc.</li> <li><code>req.url</code>: (str) The full path (e.g., <code>/users/1?active=true</code>).</li> <li><code>req.route</code>: (str) The matched app route (e.g., <code>/users/:id</code>).</li> </ul>"},{"location":"request/#data-access","title":"Data Access","text":""},{"location":"request/#url-parameters-reqparams","title":"URL Parameters (<code>req.params</code>)","text":"<p>When you define a route like <code>/users/:id:int</code>, Heaven parses it automatically.</p> <pre><code># Route: app.GET('/users/:id:int')\n# URL: /users/42\nid = req.params.get('id')\nassert isinstance(id, int)\n</code></pre> <p>Note</p> <p>Supported types: <code>:int</code>, <code>:float</code>, <code>:bool</code>, <code>:uuid</code>, <code>:date</code>, <code>:datetime</code>, <code>:str</code> (default).</p>"},{"location":"request/#query-strings-reqqueries","title":"Query Strings (<code>req.queries</code>)","text":"<p>Query parameters can also be typed in the route definition!</p> <pre><code># Route: app.GET('/search?limit:int&amp;sort:str')\n# URL: /search?limit=10&amp;sort=asc\n\nlimit = req.queries.get('limit') # 10 (int)\nsort = req.queries.get('sort') # 'asc' (str)\n</code></pre>"},{"location":"request/#json-bodies-reqdata","title":"JSON Bodies (<code>req.data</code>)","text":"<p>If you use Heaven's Schema feature, Minute 19: Schema &amp; Docs, Heaven auto-validates the body and puts the result here.</p> <pre><code># Route registered with `expects=UserSchema`\nuser = req.data\nprint(user.name)\n</code></pre>"},{"location":"request/#forms-reqform","title":"Forms (<code>req.form</code>)","text":"<p>Access <code>application/x-www-form-urlencoded</code> or <code>multipart/form-data</code> uploads.</p> <pre><code>form = req.form\nusername = form.get('username')\npassword = form.get('password')\nfile = form.get('avatar') # For file uploads\n</code></pre>"},{"location":"request/#metadata","title":"Metadata","text":"<ul> <li><code>req.headers</code>: (dict) Lowercase header dictionary.</li> <li><code>req.cookies</code>: (dict) Client cookies.</li> <li><code>req.ip</code>: (Lookup) <code>req.ip.address</code> and <code>req.ip.port</code>.</li> <li><code>req.subdomain</code>: (str) The subdomain (e.g., <code>'api'</code> or <code>'www'</code>).</li> </ul> <p>Next: You've heard them. Now answer them. On to The Response.</p>"},{"location":"response/","title":"The Response \ud83d\udde3\ufe0f","text":"<p>You have listened. Now you must speak. The <code>Response</code> object gives you the tools to reply with JSON, HTML, Files, or even silence.</p> <p>The handler signature:</p> <pre><code>async def handler(req, res, ctx):\n    ...\n</code></pre>"},{"location":"response/#the-basics","title":"The Basics","text":"<ul> <li><code>res.status</code>: (int) The HTTP status code. Defaults to <code>200</code>.</li> <li><code>res.body</code>: (bytes|str|dict|list) The content.</li> </ul> <p>Wait, <code>dict</code>? Yes. If you register a schema, Heaven handles the encoding. If not, it assumes bytes/str.</p> <pre><code>res.body = \"Hello World\" # Text\nres.body = b\"Hello World\" # Bytes\n</code></pre>"},{"location":"response/#json","title":"JSON","text":"<pre><code># Manual JSON\nres.headers = 'Content-Type', 'application/json'\nres.body = json.dumps({'msg': 'hi'})\n\n# Heaven Helper (if using Schemas)\n# Just return the object matching the schema, Heaven does the rest.\nres.body = MyUserObject() \n</code></pre>"},{"location":"response/#headers","title":"Headers","text":"<p>Headers are simple key-value tuples. You can add as many as you like.</p> <pre><code># Add one\nres.headers = 'Content-Type', 'application/json'\n\n# Add another\nres.headers = 'X-Powered-By', 'Heaven'\n</code></pre>"},{"location":"response/#helpers","title":"Helpers","text":""},{"location":"response/#resredirectlocation","title":"<code>res.redirect(location)</code>","text":"<p>Send the user somewhere else.</p> <pre><code>res.redirect('https://google.com')\n</code></pre>"},{"location":"response/#resfilepath-filenamenone","title":"<code>res.file(path, filename=None)</code>","text":"<p>Stream a file from disk. Heaven handles the content-type automatically.</p> <pre><code># Serve inline (e.g. image)\nres.file('images/cat.jpg')\n\n# Force download\nres.file('reports/finance.pdf', filename='final_report.pdf')\n</code></pre>"},{"location":"response/#resabortbody","title":"<code>res.abort(body)</code>","text":"<p>Stop everything immediately. No subsequent hooks will run.</p> <pre><code>if user.is_banned:\n    res.status = 403\n    res.abort(\"Go away.\")\n</code></pre>"},{"location":"response/#resdeferfunc","title":"<code>res.defer(func)</code>","text":"<p>Run a task after the response has been sent to the user. This is great for tasks that shouldn't block the UI but aren't complex enough for a daemon.</p> <pre><code>async def send_email(app):\n    await email_service.send(...)\n\nres.defer(send_email)\nres.body = \"Email queued!\"\n</code></pre>"},{"location":"response/#streaming-files","title":"Streaming &amp; Files","text":"<p>Heaven makes it easy to stream large responses or serve files without blocking the server event loop.</p>"},{"location":"response/#resstreamgenerator","title":"<code>res.stream(generator)</code>","text":"<p>Stream data chunk-by-chunk to the client. This is perfect for large datasets or real-time updates.</p> <pre><code>async def large_report(req, res, ctx):\n    async def generate():\n        yield \"Start of report\\n\"\n        # Simulate heavy work\n        for i in range(100):\n            yield f\"Row {i}\\n\"\n\n    # Automatically sets Transfer-Encoding: chunked\n    res.stream(generate())\n</code></pre>"},{"location":"response/#server-sent-events-sse","title":"Server-Sent Events (SSE)","text":"<p>You can also create an SSE stream easily:</p> <pre><code>res.stream(event_generator(), sse=True)\n</code></pre>"},{"location":"response/#resfilepath","title":"<code>res.file(path)</code>","text":"<p>Serve a file from the disk. Heaven uses <code>aiofiles</code> to stream the file efficiently, so even sending a 10GB file won't spike your RAM or block other requests.</p> <pre><code># Serve inline (e.g. an image)\nres.file('/var/www/image.png')\n\n# Force download\nres.file('/var/www/report.pdf', filename='Annual_Report.pdf')\n</code></pre> <p>Next: How do we share data between the router, the request, and the response? On to The Context.</p>"},{"location":"router/","title":"The Router \ud83d\udee3\ufe0f","text":"<p>The Router is the nervous system of your Heaven application. It decides where requests go, what runs before them, and what runs after.</p>"},{"location":"router/#the-basic-contract","title":"The Basic Contract","text":"<pre><code>from heaven import App\n\napp = App() # An Alias for Router()\n\napp.GET ('/users', get_users)\napp.POST('/users', create_user)\napp.PUT ('/users/:id', update_user)\napp.DELETE('/users/:id', delete_user)\n</code></pre>"},{"location":"router/#the-string-paradigm-lazy-loading","title":"The String Paradigm (Lazy Loading)","text":"<p>Heaven supports a \"String Paradigm\" that allows you to pass the import path of your handler instead of the function itself.</p> <p>This works everywhere. Routes, Hooks, Lifecycle events, and Daemons.</p>"},{"location":"router/#1-routes","title":"1. Routes","text":"<pre><code># No need to import the function!\napp.GET('/users', 'controllers.user.get_all')\napp.POST('/users', 'controllers.user.create')\napp.PUT ('/users/:id', update_user)\napp.DELETE('/users/:id', delete_user)\n</code></pre>"},{"location":"router/#2-hooks-middleware","title":"2. Hooks (Middleware)","text":"<pre><code># Instead of: from middleware.auth import check_token\napp.BEFORE('/dashboard/*', 'middleware.auth.check_token')\n</code></pre>"},{"location":"router/#3-lifecycle","title":"3. Lifecycle","text":"<pre><code># Instead of: from db import connect\napp.ON('startup', 'db.connect')\n</code></pre>"},{"location":"router/#why-use-strings","title":"Why use strings?","text":"<ol> <li>Speed: Modules are imported only when the application starts or when routes are hit.</li> <li>Cleanliness: No more 50-line import blocks at the top of your file.</li> <li>Decoupling: Solves circular import headaches instantly.</li> </ol>"},{"location":"router/#subdomains","title":"Subdomains","text":"<p>Heaven handles subdomains natively. No \"Blueprint\" confusion.</p> <pre><code># Matches: https://api.mysite.com/users\napp.GET('/users', handler, subdomain='api')\n\n# Matches: https://admin.mysite.com/dashboard\napp.GET('/dashboard', handler, subdomain='admin')\n</code></pre> <p>OR</p> <pre><code>api = app.subdomain('api')\nadmin = app.subdomain('admin')\n\n# Matches: https://api.mysite.com/users\napi.GET('/users', handler)\n\n# Matches: https://admin.mysite.com/dashboard\nadmin.GET('/dashboard', handler)\n</code></pre> <p>For more on subdomains, see Minute 7-8: Subdomains.</p>"},{"location":"router/#lifecycle-hooks-on-and-once","title":"Lifecycle Hooks: <code>ON</code> and <code>ONCE</code>","text":"<p>You have full control over the lifespan of your application.</p>"},{"location":"router/#oncefunc-or-onstartup-func","title":"<code>ONCE(func)</code> or <code>ON(STARTUP, func)</code>","text":"<p>Run code when the server starts. Perfect for database connections.</p> <pre><code>async def connect_db(app):\n    db = await Database.connect()\n    app.keep('db', db) # Persist it globally\n\napp.ONCE(connect_db)\n</code></pre>"},{"location":"router/#onshutdown-func","title":"<code>ON(SHUTDOWN, func)</code>","text":"<p>Clean up when the server stops.</p> <pre><code>async def cleanup(app):\n    db = app.peek('db')\n    await db.close()\n\napp.ON('shutdown', cleanup)\n</code></pre>"},{"location":"router/#middleware-hooks","title":"Middleware: Hooks","text":"<p>Heaven doesn't use the confusing \"middleware stack\" pattern. instead, it uses explicit <code>BEFORE</code> and <code>AFTER</code> hooks.</p>"},{"location":"router/#before","title":"<code>BEFORE</code>","text":"<p>Runs before a request hits the handler. If you abort here, the handler never runs.</p> <pre><code>async def check_auth(req, res, ctx):\n    if not req.headers.get('Authorization'):\n        res.status = 401\n        res.abort('Unauthorized')\n\n# Protects /dashboard and everything under it\napp.BEFORE('/dashboard/*', check_auth)\n</code></pre>"},{"location":"router/#after","title":"<code>AFTER</code>","text":"<p>Runs after the handler returns. Good for logging or modifying headers.</p> <pre><code>async def add_server_header(req, res, ctx):\n    res.headers = 'Server', 'Heaven/0.6'\n\napp.AFTER('/*', add_server_header)\n</code></pre>"},{"location":"router/#core-features-cors-sessions","title":"Core Features: CORS &amp; Sessions \ud83d\udee1\ufe0f","text":"<p>Heaven comes with built-in support for CORS and secure sessions.</p>"},{"location":"router/#appcors","title":"<code>app.cors()</code>","text":"<p>Enable Cross-Origin Resource Sharing with a single line.</p> <pre><code>app.cors(origins='https://myapp.com', methods=['GET', 'POST'])\n</code></pre>"},{"location":"router/#appsessions","title":"<code>app.sessions()</code>","text":"<p>Enable signed, secure cookie-sessions.</p> <pre><code>app.sessions(secret_key='keep-it-secret')\n\n# In your handler:\n# ctx.session.user_id = 123\n</code></pre>"},{"location":"router/#daemons-background-tasks","title":"Daemons: Background Tasks \ud83d\udc7b","text":"<p>Heaven has a built-in process manager for background tasks. No Celery required.</p> <p>Warning</p> <p>Heaven is single-threaded. Never block the main loop with <code>time.sleep()</code>.</p>"},{"location":"router/#creating-a-daemon","title":"creating a Daemon","text":"<p>A daemon is a function that receives the <code>app</code> instance. If it returns a number <code>N</code>, it sleeps for <code>N</code> seconds and runs again.</p> <pre><code>async def heartbeat(app):\n    print(\"Lub-dub...\")\n    # Sleep 5 seconds, then repeat\n    return 5\n\napp.daemons = heartbeat\n</code></pre>"},{"location":"router/#mounting-applications","title":"Mounting Applications","text":"<p>You can mount entire other Heaven apps onto your main app. This is how you build modular monoliths.</p> <pre><code>from heaven import App\nfrom my_blog import blog_app\nfrom my_store import store_app\n\nmain = App()\n\n# Mounts blog_app at /blog\nmain.mount(blog_app) \n\n# Note: Mounting merges routes.\n# If blog_app had a route '/posts', it is now accessible on main at '/posts'\n# Wait, let's clarify alignment with minute 8.\n</code></pre> <p>Tip</p> <p>Use <code>mount</code> to split your code into multiple files/modules, then simply combine them in <code>main.py</code>.</p> <p>Next: Now that we know how to route the request, let's learn how to read it. On to Minute 4: The Request.</p>"},{"location":"schema/","title":"Schema &amp; API Docs","text":"<p>Heaven doesn't just run your code; it understands it. By using schemas, you get instant validation, auto-generated documentation, and type safety, all powered by the incredibly fast <code>msgspec</code>.</p>"},{"location":"schema/#defining-schemas","title":"Defining Schemas","text":"<p>A schema is a class that describes your data structure. Heaven exports <code>Schema</code> (a wrapper around <code>msgspec.Struct</code>) and <code>Constraints</code> (a wrapper around <code>msgspec.Meta</code>) to help you define validation rules.</p> <pre><code>from typing import Annotated\nfrom heaven import Schema, Field\n\nclass User(Schema):\n    id: int\n    name: str\n\n    # 1. Bounds (Values vs Lengths)\n    # Use 'min'/'max' for numeric values (&gt;=, &lt;=)\n    age: Annotated[int, Field(min=18, max=100, desc=\"Must be legal age\")]\n\n    # Use 'min_len'/'max_len' for sequence lengths\n    tags: Annotated[list[str], Field(min_len=1, desc=\"At least one tag required\")]\n\n    # 2. Formats\n    # Built-in support for 'email', 'uuid', and 'slug'\n    email: Annotated[str, Field(format=\"email\", example=\"ray@heaven.com\")]\n    apikey: Annotated[str, Field(format=\"uuid\", error_hint=\"Invalid API Key format\")]\n    slug: Annotated[str, Field(format=\"slug\")]\n\n    # 3. Steps (Multiples)\n    duration: Annotated[int, Field(step=15)]\n\n    # 4. Defaults (via msgspec.field)\n    is_active: bool = True\n    metadata: dict = Schema.Field(default_factory=dict)\n</code></pre>"},{"location":"schema/#the-field-helper","title":"The <code>Field</code> Helper","text":"<p>Heaven provides a smart <code>Field()</code> helper that simplifies validation. It returns <code>msgspec.Meta</code> constraints configured for your needs.</p> Argument Maps To (msgspec) Description <code>min</code> / <code>max</code> <code>ge</code> / <code>le</code> Numeric constraints (&gt;=, &lt;=) <code>min_len</code> / <code>max_len</code> <code>min_length</code> / <code>max_length</code> Sequence length constraints <code>step</code> <code>multiple_of</code> Number must be a multiple of X <code>format</code> <code>pattern</code> Presets: <code>\"email\"</code>, <code>\"uuid\"</code>, <code>\"slug\"</code> <code>desc</code> <code>description</code> Field description for OpenAPI <code>example</code> <code>extra_json_schema</code> Example value for docs <code>error_hint</code> <code>extra_json_schema</code> Custom error message hint"},{"location":"schema/#advanced-patterns","title":"Advanced Patterns","text":"<p>Heaven's <code>Field</code> helper is designed to scale with your needs. It seamlessly integrates with <code>msgspec</code>'s advanced features.</p>"},{"location":"schema/#1-nested-arrays-matrices","title":"1. Nested Arrays &amp; Matrices","text":"<p>You can apply constraints to the list itself AND the items within it.</p> <pre><code>class BatchUpload(Schema):\n    # The LIST must have 1-100 items.\n    # Each ITEM must be a string of length 3-50.\n    tags: Annotated[\n        list[Annotated[str, Field(min_len=3, max_len=50)]], \n        Field(min_len=1, max_len=100)\n    ]\n</code></pre>"},{"location":"schema/#2-complex-unions-polymorphism","title":"2. Complex Unions (Polymorphism)","text":"<p>Validate data that can be one of multiple types, each with its own rules.</p> <pre><code>class SearchQuery(Schema):\n    # ID can be a positive Integer OR a UUID String\n    id: Annotated[int, Field(min=1)] | Annotated[str, Field(format=\"uuid\")]\n</code></pre>"},{"location":"schema/#3-timezones-dates","title":"3. Timezones &amp; Dates","text":"<p>Native <code>msgspec</code> constraints (like <code>tz</code>) pass straight through <code>Field</code>.</p> <pre><code>from datetime import datetime\n\nclass Event(Schema):\n    # Enforce timezone-aware datetimes (rejects naive inputs)\n    start_time: Annotated[datetime, Field(tz=True)]\n</code></pre>"},{"location":"schema/#4-the-escape-hatch-raw-access","title":"4. The Escape Hatch (Raw Access)","text":"<p>If you strictly prefer raw <code>msgspec</code>, you can bypass <code>Field</code> entirely.</p> <pre><code>from heaven import Constraints\n\nclass RawMetal(Schema):\n    # Pure msgspec code works 100% of the time\n    data: Annotated[bytes, Constraints(min_length=10)]\n</code></pre>"},{"location":"schema/#the-schema-registry","title":"The Schema Registry","text":"<p>Instead of cluttering your handlers with decorators, Heaven uses a \"Sidecar\" pattern. You register schemas on the router's <code>schema</code> property.</p> <pre><code># 0. You can mount schemas on subdomains e.g.\napi = app.subdomain('api')\napi.schema.POST(...)\n\n# 1. Or on the default subdomain i.e. `www`\napp.schema.POST('/users', \n    expects=User, \n    returns=User, \n    title=\"Create User\",\n    summary=\"Creates a new user in the system\"\n)\n\n# 2. Then in your route handler(s)\nasync def create_user(req, res, ctx):\n    # Validated 'User' object injected into `data` by heaven\n    user = req.data\n\n    # maybe some database logic?...\n\n    # Heaven auto-converts this back to JSON\n    res.body = user\n\n# 3. Look ma, no decorators!\napp.POST('/users', create_user)\n</code></pre>"},{"location":"schema/#validation","title":"Validation","text":"<p>When you register an <code>expects</code> schema, Heaven automatically:</p> <ol> <li>Validates the incoming JSON body against the schema.</li> <li>Aborts with <code>422 Unprocessable Entity</code> if it's invalid (with a nice error message).</li> <li>Populates <code>req.data</code> with the validated object.</li> </ol>"},{"location":"schema/#auto-generated-docs-openapi","title":"Auto-Generated Docs (OpenAPI)","text":"<p>Heaven can generate a stunning interactive API reference website for you.</p> <pre><code># Mount the docs at /docs\napp.DOCS('/docs', title=\"My API\", version=\"1.0.0\")\n</code></pre> <p>Now visit <code>http://localhost:8000/docs</code> in your browser. You will see a beautiful Scalar UI where you can test your endpoints.</p>"},{"location":"schema/#advanced-subdomains","title":"Advanced: Subdomains","text":"<p>You can mount docs on a specific subdomain.</p> <pre><code>app.DOCS('/docs', subdomain='api')\n</code></pre>"},{"location":"schema/#advanced-output-protection","title":"Advanced: Output Protection","text":"<p>You can also control how strict Heaven is about what you send back.</p> <pre><code>app.schema.GET('/users/:id', \n    returns=User,\n    protect=True,  # Strip fields not in User schema\n    strict=True    # Error 500 if a required field is missing\n)\n</code></pre> <ul> <li><code>protect=True</code>: Prevents data leaks. If your DB returns <code>password_hash</code> but your Schema doesn't have it, it won't be sent.</li> <li><code>partial=True</code>: Allows sending only a subset of fields (good for PATCH updates).</li> </ul> <p>Pro Tip: Performance</p> <p>Heaven's <code>Schema</code> is powered by msgspec, widely considered the fastest JSON library for Python. While standard features cover 99% of cases, you can leverage the full power of <code>msgspec</code> directly if you need e.g. zero-copy decoding or Struct caching.</p> <p>Next: Yay!!! You know json kung fu. But how well? On to API Docs.</p>"},{"location":"security/","title":"Security &amp; Signing","text":"<p>Heaven takes security seriously. It provides strict, type-safe tools for handling sensitive data, signatures, and secure cookies.</p>"},{"location":"security/#secure-serializer","title":"Secure Serializer","text":"<p>The <code>heaven.security.SecureSerializer</code> class is a high-performance alternative to <code>itsdangerous</code>. It uses <code>msgspec</code> for ultra-fast JSON serialization and <code>HMAC-SHA256</code> for signing.</p>"},{"location":"security/#basic-usage","title":"Basic Usage","text":"<pre><code>from heaven.security import SecureSerializer\n\n# Initialize with a secret key\nsigner = SecureSerializer(secret_keys=\"my-super-secret-key\")\n\n# data -&gt; signed token\ntoken = signer.dumps({\"user_id\": 123, \"role\": \"admin\"})\nprint(token) \n# Output: eyJ1c2... . ... .signature\n\n# token -&gt; data\ndata = signer.loads(token)\nprint(data)\n# Output: {'user_id': 123, 'role': 'admin'}\n</code></pre>"},{"location":"security/#expiration-max-age","title":"Expiration (Max Age)","text":"<p>You can enforce expiration on tokens.</p> <pre><code>try:\n    # Valid only for 60 seconds\n    data = signer.loads(token, max_age=60)\nexcept SignatureExpired:\n    print(\"Token is too old!\")\nexcept BadSignature:\n    print(\"Don't try to hack me!\")\n</code></pre>"},{"location":"security/#key-rotation","title":"Key Rotation","text":"<p>Heaven supports key rotation out of the box. Pass a list of keys to the constructor. </p> <ul> <li>First Key: Used for signing new data.</li> <li>All Keys: Used for verifying incoming data.</li> </ul> <p>This allows you to rotate secrets without logging out all active users.</p> <pre><code># [New Key, Old Key]\nsigner = SecureSerializer(secret_keys=[\"new-secret-2025\", \"old-secret-2024\"])\n</code></pre>"},{"location":"security/#strict-schema-validation","title":"Strict Schema Validation","text":"<p>Because <code>SecureSerializer</code> uses <code>msgspec</code> under the hood, you can enforce a schema on the decoded data.</p> <pre><code>from msgspec import Struct\n\nclass UserToken(Struct):\n    user_id: int\n    role: str\n\n# Returns a UserToken object, or raises error if structure doesn't match\ndata = signer.loads(token, type=UserToken)\n</code></pre>"},{"location":"security/#context-security","title":"Context Security","text":"<p>Heaven's <code>Context</code> object (<code>ctx</code>) is protected against accidental overwrites of critical keys.</p> <pre><code>async def handler(req, res, ctx):\n    ctx.my_data = 123 # OK\n\n    ctx.session = \"hacked\" # raises AttributeError!\n    # 'session', 'app', 'request', 'response' are reserved.\n</code></pre> <p>Next: It works locally. Let's show the world. On to Deployment.</p>"},{"location":"snippets/","title":"Minute 9: Centralized Middleware &amp; Snippets","text":"<p>Heaven is extremely unopinionated, but it provides powerful tools for centralized control. This section shows how to use <code>.BEFORE</code> hooks for common tasks like Authentication and Data Validation.</p>"},{"location":"snippets/#centralized-authentication","title":"Centralized Authentication","text":"<p>Instead of littering your handlers with decorators, use <code>.BEFORE</code> to protect entire route trees at once.</p> <pre><code>from http import HTTPStatus\nfrom heaven import App, Request, Response, Context\n\napp = App()\n\n# 1. Define your protection logic\nasync def protect(req: Request, res: Response, ctx: Context):\n    token = req.headers.get('authorization')\n\n    # Use your preferred JWT or other validation scheme here\n    if not token or token != \"secret-token\":\n        res.status = HTTPStatus.UNAUTHORIZED\n        res.abort('Unauthorized Access')\n        return\n\n    # Keep the user in context for the actual handler\n    ctx.keep('user', {\"id\": 1, \"name\": \"Raymond\"})\n\n# 2. Register it globally or for specific route patterns\napp.BEFORE('/api/v1/*', protect)\n\n# 3. Your handler stays clean and focused\nasync def get_secure_data(req: Request, res: Response, ctx: Context):\n    user = ctx.user # Already populated by the hook\n    res.body = {\"data\": \"Top Secret\", \"for\": user['name']}\n\napp.GET('/api/v1/data', get_secure_data)\n</code></pre>"},{"location":"snippets/#centralized-data-validation","title":"Centralized Data Validation","text":"<p>You can also use <code>.BEFORE</code> to validate incoming data before it ever reaches your handler.</p> <pre><code>import json\nfrom heaven import App, Request, Response, Context\n\napp = App()\n\nasync def validate_json(req: Request, res: Response, ctx: Context):\n    try:\n        data = json.loads(req.body)\n        if \"email\" not in data:\n            raise ValueError(\"Email is required\")\n        ctx.keep('payload', data)\n    except Exception as e:\n        res.status = 400\n        res.abort(f\"Invalid Data: {str(e)}\")\n\napp.BEFORE('/api/v1/login', validate_json)\n\nasync def login(req: Request, res: Response, ctx: Context):\n    payload = ctx.payload\n    print(f\"Logging in {payload['email']}\")\n    res.body = {\"status\": \"ok\"}\n\napp.GET('/api/v1/login', login)\n</code></pre> <p>Tip</p> <p>Use <code>.BEFORE('*', handler)</code> to run a hook for every single request in your application (e.g., for logging or CORS).</p>"},{"location":"subdomains/","title":"Subdomains","text":"<p>Heaven makes it incredibly easy to build multi-tenant applications or segregate your API using subdomains.</p>"},{"location":"subdomains/#basics","title":"Basics","text":"<p>The <code>app.subdomain(name)</code> method returns a proxy object (a <code>SubdomainContext</code>) that allows you to register routes, hooks, and schemas specifically for that subdomain.</p> <pre><code># Create a proxy for the 'admin' subdomain\nadmin = app.subdomain('admin')\n\n# Register a route\nadmin.GET('/dashboard', admin_dashboard)\n</code></pre> <p>Now, <code>http://admin.example.com/dashboard</code> will serve the dashboard, but <code>http://example.com/dashboard</code> will 404.</p>"},{"location":"subdomains/#consistency-proxies","title":"Consistency &amp; Proxies","text":"<p>It is important to understand that <code>app.subdomain('name')</code> returns a new proxy object each time, but they all point to the same shared state within the application.</p> <p>This means you can call <code>app.subdomain('api')</code> in multiple places (even different files), and they will all contribute to the same routing table.</p> <pre><code># In users.py\napi = app.subdomain('api')\napi.GET('/users', list_users)\n\n# In posts.py\napi = app.subdomain('api') # This is safe!\napi.GET('/posts', list_posts)\n</code></pre> <p>Both <code>/users</code> and <code>/posts</code> are immediately registered to the <code>api</code> subdomain. There is no need to pass the <code>api</code> object around; you can just re-instantiate the proxy whenever you need it.</p>"},{"location":"subdomains/#subdomain-schemas","title":"Subdomain Schemas","text":"<p>You can also enforce schemas specifically for a subdomain. This is useful when your main site and your API have different validation rules or when you want formatted OpenAPI docs for your API subdomain.</p> <p>The <code>SubdomainContext</code> exposes a <code>.schema</code> property which, similarly, returns a consistent proxy to the registry.</p> <pre><code>from heaven import Schema\n\nclass CreateUser(Schema):\n    username: str\n\napi = app.subdomain('api')\n\n# Register schema-validated route on the API subdomain\napi.schema.POST('/users', expects=CreateUser)\napi.POST('/users', create_user_handler)\n</code></pre> <p>Just like the router itself, <code>api.schema</code> is safe to call multiple times.</p>"},{"location":"subdomains/#hooks","title":"Hooks","text":"<p>You can also register middleware (hooks) that only run for a specific subdomain.</p> <pre><code>admin = app.subdomain('admin')\n\nasync def require_admin(req, res, ctx):\n    if not user.is_staff:\n        res.status = 403\n        res.abort(\"Forbidden\")\n\n# This hook ONLY runs for requests to admin.example.com\nadmin.BEFORE('/*', require_admin)\n</code></pre>"}]}